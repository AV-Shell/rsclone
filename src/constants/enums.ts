// eslint-disable-next-line no-shadow
enum ButtonType {
  REPEAT = 'repeat',
  HARD = 'hard',
  NORMAL = 'normal',
  EASY = 'easy',
}

// eslint-disable-next-line import/prefer-default-export
export { ButtonType };
